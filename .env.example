# SQLite database file (Drizzle ORM)
DATABASE_URL=local.db

# Financial Modeling Prep (FMP) API key.
# Server-side only — read via $env/dynamic/private, never exposed to the client.
FMP_API_KEY=your-fmp-api-key-here
